"MP3 Downloads/"
"MP4 Downloads/"

assets/settings.json

*.mp4
*.mp3
*.png
*.jpg
# Bundled tool binaries: shipped inside the release zip, never committed.
# Get-ToolPath prefers bin/ and falls back to PATH, so a source checkout works
# either by dropping the exes in here or by having them installed.
bin/*.exe

# Project saves (the editor's .ffgui.json store) -- user data, never committed.
assets/projects/
