What's New in This Update:

• Fixed "running scripts is disabled on this system" errors when launching the tool on a fresh Windows install
• Launcher.exe now works without changing any PowerShell settings
• If you still see the error when running the script directly, launch with Launcher.exe or "assets/Launcher Config.bat" instead
